# Structured review — 2026-06-21 (VDSim-Thesis)

Tag: `thesis-review-2026-06-21`  
Branch: `VDSim-Thesis`  
Gate: **402/402 ctest green**

Process spec: [`docs/design/STRUCTURED_REVIEW.md`](../design/STRUCTURED_REVIEW.md)  
Machine log: [`2026-06-21/ctest_summary.txt`](2026-06-21/ctest_summary.txt)  
Matrix JSON: [`2026-06-21/verification_matrix.json`](2026-06-21/verification_matrix.json)

## Method

Three parallel readonly agent lanes (Plant / Core session / Catalog) + human synthesis.
Each lane produced BUGS · SIMPLIFICATION · VERIFICATION_MATRIX. Fixes applied on thesis
branch; simplification limited to dead code and doc/contract alignment.

## Fixes applied (code)

| ID | Severity | Fix | Structural check |
|----|----------|-----|------------------|
| R1 | HIGH | `build_complete` ignores optional per-wheel tire slots | `assembly_api.test_build_complete_*` |
| R2 | MED | `part_compat` + `part_card_stats` for `front_chassis`/`rear_chassis`/`body` retax | `assembly_api` + manual compat |
| R3 | MED | `make_direct_control_session` forces `plant_path=true` | `VlaPlant.*` (402 green) |
| R4 | LOW | `vdsim_plant` docstring: drop unimplemented `Mz`; remove dead symbols | `vla_plant` smoke |
| R5 | MED | Python grip-loss smoke: vehicle `ΣFx` vs `Fx_cmd` (C++ parity) | `test_patch_brake_turn_grip_loss` |

## Deferred (documented gaps)

| Gap | Risk | Next step |
|-----|------|-----------|
| `CmdL1` on non-direct `SimSession` silently pedal-mapped | Footgun for cosim | Runtime assert or doc guard |
| Blueprint change wipes custom `parts` | UX data loss | Merge policy in `catalog_bridge` |
| `powertrain` slot missing from `slots.py` | Split blueprint not editable in GUI | Add slot or drop category |
| Per-corner tire Δ in assembly preview | Misleading stats | Resolve all corner tires in `_resolve_summary` |

## Simplification applied

- Removed `WHEEL_NAMES`, `_EPS` from `vdsim_plant.py`
- Dropped unused `base_mu` args from friction validators
- Removed unused `level_rank` import from `assembly.py`

## Lane summary

| Lane | Regex smoke | Result |
|------|-------------|--------|
| Plant | `VlaPlant`, `vla_plant` | PASS |
| Core session | `PerWheel`, `PerAxle`, `SimSession`, `DragCoast` | PASS |
| Catalog | `catalog`, `assembly`, `blueprint`, `multi_vehicle` | PASS |
| **Full** | 402 tests | **PASS** |

## Reproduce

```bash
cmake --build build -j
cd build && ctest --output-on-failure
python3 tools/review_evidence_bundle.py --tag thesis-review-2026-06-21
python3 tests/scripts/test_vla_plant.py
```

## Conclusion

No ISO/benchmark re-baseline. Thesis plant contract is structurally covered by 15× `VlaPlant`
gtest + Python acceptance bundle. Catalog assembly false “incomplete build” on L2 is fixed and
now gated by automated test.
