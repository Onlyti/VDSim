# VDSim 세션 핸드오프 — 2026-06-15 (오후)

## 목표

아키텍처 설계 논의 + P1 구현: VehNetwork / ISteeringKinematics / DrivetrainOutput.brake_absorbed

---

## 현재 상태

branch: **main** · HEAD: `3399d1e` · ctest: **339/339 green**

---

## 완료

### 인프라 / 설계 논의

- 설계 문서: `docs/design/CONTROLLER_MODULE.md`
  - VehNetwork Module (차량 네트워크 — deadtime + drop)
  - LcLon / LcLat 계층 (L1~L8, 확정)
  - Subsystem = cascade + 물리 담당
  - SteeringOutput Kinematic/Dynamic mode
  - ISteeringKinematics 인터페이스
  - DrivetrainOutput.brake_absorbed (EV regen 분기)
  - 순차 실행: IDrivetrain → IBrakeSystem(잔여)

### P1 구현 (`3399d1e`)

1. `core/include/vdsim/veh_network.hpp`
   - `IVehNetwork` 인터페이스
   - `DefaultVehNetwork`: Gaussian deadtime `N(μ, σ²)` + Bernoulli drop + OnDrop 정책
   - `VehNetworkParams`: deadtime_mean/std, drop_rate, on_drop, seed

2. `core/include/vdsim/steering_kinematics.hpp`
   - `ISteeringKinematics`: rack_travel → FL/FR wheel angles
   - `RatioSteeringKinematics`: 단일 비율 (Ld1/2)
   - `AckermannKinematics`: FL/FR 독립 비율 (Ld3)

3. `core/include/vdsim/subsystems.hpp`
   - `DrivetrainOutput.brake_absorbed [0,1]` 추가
   - `SteeringOutput.mode` (Kinematic/Dynamic) + `motor_force` 추가

4. `core/src/seven_dof_dynamics.cpp` + `free_3d_dynamics.cpp`
   - IDrivetrain → brake_absorbed → effective_brake → IBrakeSystem 순차 실행

### 기타 오늘 완료

- sensors[] 파서 (79467af)
- path trajectory + pure-pursuit v2 (6175d0a)
- comms json template (c2cee1d)
- sim: 통합 블록 (4094e9d)
- campaign runner + presets (faf3010)
- P1 Simulink/FMI (23bc289)
- P2+P3 MATLAB bridge + tire validation (c2cee1d)
- diff_open/locked/lsd catalog parts (6542b84)
- subsystem delay 일원화 (7822b55)

---

## 미완 (다음 할 일)

### P2 구현 (설계 확정, 구현 미착수)

| 항목 | 파일 | 내용 |
|------|------|------|
| Rack EOM → State 확장 | `core/include/vdsim/state.hpp` | `rack_travel`, `rack_velocity` 추가 |
| Rack EOM RK4 적분 | `seven_dof_dynamics.cpp` | `F_tire_feedback = Mz/R_pinion + Fy×trail` |
| SteeringOutput.Dynamic 모드 연결 | `seven_dof_dynamics.cpp` | motor_force → rack EOM |
| LcLon/LcLat typed commands | `control.hpp` | variant 확장, 횡방향 계층 struct |
| Subsystem min/max level 선언 + 에러 | `subsystems.hpp` | runtime check |
| VehNetwork → SimSession 연결 | `sim_session.hpp/.cpp` | `set_input` 앞에 삽입 |
| ISteeringKinematics → 동역학 연결 | `seven_dof_dynamics.cpp` | `rack_travel → FL/FR angles` |

---

## 주의·함정

- `SteeringOutput.roadwheel_angle`: 하위호환용 필드 남아 있음. ISteeringKinematics 연결 후 이 필드에서 계산됨.
- `VehNetwork`는 아직 `SimSession`에 연결 안 됨 (인터페이스/구현만 추가). `sim_session.cpp`의 `set_input()` 앞에 `network_->apply()` 호출 추가 필요.
- `brake_absorbed` 기본값 0.0 → 기존 ICE 동작 변화 없음.
- Rack EOM은 P2 범위 — Kinematic 모드가 기본, Dynamic은 Ld3+ 옵션.
- `veh_network.hpp`는 header-only (impl 포함) → 별도 .cpp 불필요.

---

## 관련 경로

```
docs/design/CONTROLLER_MODULE.md   전체 아키텍처 설계 문서
core/include/vdsim/veh_network.hpp IVehNetwork + DefaultVehNetwork
core/include/vdsim/steering_kinematics.hpp ISteeringKinematics
core/include/vdsim/subsystems.hpp  DrivetrainOutput.brake_absorbed, SteeringOutput.mode
core/src/seven_dof_dynamics.cpp    순차 brake 적용
core/src/free_3d_dynamics.cpp      순차 brake 적용
```
