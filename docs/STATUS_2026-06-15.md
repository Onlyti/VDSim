# VDSim 개발 현황 (2026-06-15)

**Branch:** main · **Commit:** `6175d0a` · **Tests:** 339/339 ctest green

---

## 이번 세션 (2026-06-15) — config 아키텍처 + 실험 API 완성

### ✅ 완료 (7 커밋, 3개 우선순위)

#### A. Config 가이드 + 스키마 확정
- 시나리오 계층 확정: `scenario { sim, map, comms, run, agents[] }`
- 에이전트 계층: `agent { vehicle { blueprint, parts, sensors[] }, spawn, path, control }`
- **docs/CONFIG_GUIDE.md** 작성

#### B. Per-agent control (internal/external)
- `VehicleSpawn.control` 파서 추가
- realtime_server: internal = speed-hold cruise v1, external = UDP + failsafe
- **Commit: 852c4fe**

#### C. Scenario-level comms 라우터
- `CommsConfig` + `CommsChannel` (TX fan-out / RX fan-in)
- VDS1 protocol v4: vehicle_id 헤더로 다중 차량 demux
- **Commit: 16e39f7**
- 스모크: TX 296 packets, RX cmds 10 routed ✓

#### D. 실험 Python API — `class Sim`
- 4개 seam: `state() / measurements() / set_input() / run_core_dt()`
- 5개 피드백 항목:
  1. `state()['beta']`: vehicle sideslip
  2. `log_extra(dict)`: per-step 컨트롤러 변수 → CSV
  3. `reset(v0, ...)`: plant 재사용 (ablation)
  4. `register_metric(name, fn)`: custom metric
  5. `plot_comparison(runs)`: A/B/C overlay
- **Commit: 0ef34b8, c0794b8**
- 예제 2개: quickstart + path_follow (pure-pursuit)

#### E. User-defined reference point
- `Sim(ref_point="rear_axle" / "front_axle" / [rx, ry])`
- 위치/속도/가속도 강체 변환 (ISO 8855)
- **Commit: 84d3f7b**

#### F. Per-vehicle sensors[] parser
- Inline list: `[{type: gnss, noise_std: 0.3}, ...]`
- 또는 scalar path: `sensors: sensors.yaml`
- type → SensorNoise 필드 자동 매핑
- **Commit: 79467af**

#### G. Path trajectory parser + pure-pursuit v2
- Trajectory yaml: `points: [[x, y, vx], ...]` (순수 경로 데이터)
- `lookahead` 분리: scene vehicle의 `path_lookahead:` 파라미터
- Internal controller v2: path 있으면 pure-pursuit + per-point 속도
- Sample: configs/paths/oval.yaml (80 pts)
- **Commit: 6175d0a**
- 스모크: 3.5s 동안 x 47m 이동 ✓

### 📊 코드 변화
- `python/vdsim_lab.py`: +357 lines (Sim class, plot, register_metric, ref_point)
- `cosim/world_scenario.hpp`: +10 lines (CommsConfig, sensors, path fields)
- `cosim/realtime_server.cpp`: +100 lines (pure-pursuit, Waypoint, load_waypoints)
- 테스트: experiment_api (6 tests) + multi_vehicle (4 tests) = 10 new tests

---

## 전체 프로젝트 현황

### 🎯 Shipped (v0.5.1 기준)

| 영역 | 현황 | 상태 |
|------|------|------|
| **Dynamics** | L1 bicycle, L2 7DOF, L3 14DOF, L4 hardpoint kin, L5 3D+terrain | ✅ |
| **Tire** | MF96 default, MF2002 `.tir` backend, LuGre brush, belt transient | ✅ |
| **Drivetrain** | Open diff, engine inertia, 2D map + gearbox v2, shift policy | ✅ |
| **Brake/Steer** | Proportional brake + EBD, ratio steer, deadtime modules | ✅ |
| **Suspension** | Linear spring/damper, ARB per-wheel, Ld4 hardpoint, multibody M1–M7 | ✅ |
| **Co-sim** | VDS1 UDP (v4, multi-vehicle), FMI 2.0, multi-vehicle single process | ✅ |
| **Validation** | ISO 7401/4138/3888, 339 ctests, re-baseline CI-gated | ✅ |
| **GUI** | 3-tab scene UI, catalog, workshops (susp/brake/steer/drivetrain) | ✅ |
| **Catalog** | Parts manifest, blueprints, fleet materialize | ✅ |

### 🔄 v0.5.2 In-Progress (이번 세션)

| 영역 | 항목 | 진행 |
|------|------|------|
| **Config** | Scenario hierarchy, per-vehicle sensors, path trajectory | ✅ 완료 |
| **Experiment API** | Sim seam, ablation tools (log_extra/reset/register_metric), ref_point | ✅ 완료 |
| **Comms** | VDS1 TX/RX routing, scenario-level spec | ✅ 완료 |
| **GUI** | Terrain Play (M4), stunt authoring (M5c), `.tir` import | ⏳ 미완 |
| **Comms** | json/nmea_gga template, sensor.* TX | 🔴 스킵 |
| **Runtime** | sim: {dt, t_end, time_scale} 통합 블록 | 🔴 미완 |

### 🚫 Backlog (v0.5.2+)

| 우선 | 항목 | 영역 | 비고 |
|------|------|------|------|
| 🔴 | comms json/nmea_gga template · sensor.* TX | cosim | realtime_server 내 skip → 실현 필요 |
| 🔴 | scenario-level `sim: {dt, t_end, time_scale}` | config | 현재 rate/cmd_timeout 분산 |
| 🟡 | campaign runner (configs/experiments/ + maps/ + sensors/) | batch | 골격만 있음 |
| 🟡 | GUI multi-vehicle spawn + demux + render | GUI | #158 |
| 🟡 | GUI terrain Play (M4) | GUI | render-only, play-authoring 필요 |
| 🟡 | GUI stunt authoring (M5c) | GUI | M3 jump 있으나, ramp/loop authoring 미완 |
| 🟡 | GUI `.tir` import (T1 tail) | GUI | template 없음 |
| 🟡 | Engine map workshop (torque–RPM curves) | GUI | WS2-6 stub |
| ❓ | C++ module plugins (workshop, `.so` build/check) | subsystem | Design doc 있음 (plan) |
| ❓ | V2V collision | dynamics | 계획만 (v0.4 descope) |
| ❓ | Published commercial cross-val | validation | "honest gap today" |

---

## 로컬 진행 상황 (세션 기준)

### 시작: 6월 15일 06:30
- 컨텍스트 길어짐 → merge + fresh 재시작
- handoff: sensors 파서 / path trajectory 파서 / comms json template 우선순위

### 진행
1. **sensors 파서** (79467af) — 60분
   - `agents.vehicle.sensors[]` → `SensorParams`
   - type별 매핑 + noise_std/bias/bias_rw
   - 4/4 test pass

2. **path trajectory 파서 + pure-pursuit v2** (6175d0a) — 90분
   - trajectory yaml: points 만 포함 (순수 경로)
   - lookahead는 scene 파라미터로 분리
   - 근접-anchor + lookahead pure-pursuit 구현
   - 3.5s smoke test: x 47m 이동 확인

### 완료: 06:50
- 339/339 ctest green
- 두 작업 모두 스모크 + 유닛 테스트 확인
- git push (main `6175d0a`)

---

## 다음 단계 (v0.5.2 MVP 완성)

### 즉시 (이 세션)
- [ ] comms **json template** (realtime_server skip → 구현)
  - State를 compact JSON으로 → HTTP/MQTT bridge 용
  - 1–2일
- [ ] scenario-level **`sim:` 통합 블록**
  - dt / t_end / time_scale 한곳에 모으기
  - batch/cosim/Sim API 모두에 영향 (설계 먼저)
  - 2–3일

### 단기 (v0.5.2 feature complete)
- [ ] campaign runner 콘텐츠 (experiments yaml, preset maps, sensor configs)
- [ ] GUI multi-vehicle 실装 (#158)

### 중기 (v0.5.2 GUI)
- [ ] GUI terrain Play (M4) + stunt authoring (M5c)
- [ ] GUI `.tir` import
- [ ] Workshop: engine map editor

### 장기 (v0.6+)
- [ ] C++ module plugin system (plan in place)
- [ ] V2V collision
- [ ] Published benchmark
