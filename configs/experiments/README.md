# Experiment (scenario) configs

Composed vehicle + tire + level + map + maneuver + sensors + duration. Authored by the Experiment Builder (`builder/`); run via `vdsim_lab.Experiment.from_config(name)`. Schema: see builder/README.md.
