# Sensor suite configs

Per-sensor pose + type + noise. Authored by the Experiment Builder (`builder/`). gnss/imu/wheel/steer map into the in-sim SensorParams; camera/lidar mounts are for the render coupling. Schema: see builder/README.md.
