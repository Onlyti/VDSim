id: body.sedan
type: body
version: 1
schema: body_v1
label: Sedan chassis body
tags:
- body
body:
  wheelbase: 2.7
  inertia_diag:
  - 500.0
  - 2000.0
  - 2500.0
  unsprung_mass:
  - 40
  - 40
  - 40
  - 40
  cg_to_front: 1.2
  mass_sprung: 1350.0
  track_front: 1.55
  track_rear: 1.55
  mass: 1500.0
  wheel_radius_nominal: 0.32
  cg_height: 0.55
  cg_to_rear: 1.5
  # Placeholder body box and wheel width for the 3D replay renderer (trace
  # schema 0.3, 11_trace_contract_spec 3.1.1). No source: these are not the
  # dimensions of any vehicle and must not be quoted as a specification. They
  # were picked to look consistent with this preset's own wheelbase and track
  # so the drawn primitives do not float away from the wheels. Nothing but the
  # renderer reads them -- no equation does.
  body_lwh_m: [4.60, 1.80, 1.45]
  wheel_width_m: 0.225
